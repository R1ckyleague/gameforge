# Animated Wallpaper — dependencias Python
# Ejecuta:  pip install -r requirements.txt
#
# VLC NO es necesario — si lo tienes instalado se usa automáticamente
# para aceleración por hardware. Si no, se usa OpenCV (incluido aquí).

pywin32>=306
opencv-python>=4.8.0
numpy>=1.24.0
customtkinter>=5.2.0
tkinterdnd2>=0.3.0
Pillow>=10.0.0

# Opcional — solo si tienes VLC y quieres usarlo como motor principal:
# python-vlc>=3.0.18122
